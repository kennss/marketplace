//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

/// Accounts.
pub struct RevokeCommunityCollection {
    pub registration: solana_program::pubkey::Pubkey,
    /// Must match `registration.leader_wallet`.
    pub leader: solana_program::pubkey::Pubkey,
}

impl RevokeCommunityCollection {
    pub fn instruction(&self) -> solana_program::instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_program::instruction::AccountMeta],
    ) -> solana_program::instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.registration,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            self.leader,
            true,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = RevokeCommunityCollectionInstructionData::new()
            .try_to_vec()
            .unwrap();

        solana_program::instruction::Instruction {
            program_id: crate::TENSOR_MARKETPLACE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshDeserialize, BorshSerialize)]
pub struct RevokeCommunityCollectionInstructionData {
    discriminator: [u8; 8],
}

impl RevokeCommunityCollectionInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [183, 235, 152, 95, 37, 136, 110, 172],
        }
    }
}

impl Default for RevokeCommunityCollectionInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `RevokeCommunityCollection`.
///
/// ### Accounts:
///
///   0. `[writable]` registration
///   1. `[signer]` leader
#[derive(Clone, Debug, Default)]
pub struct RevokeCommunityCollectionBuilder {
    registration: Option<solana_program::pubkey::Pubkey>,
    leader: Option<solana_program::pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_program::instruction::AccountMeta>,
}

impl RevokeCommunityCollectionBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn registration(&mut self, registration: solana_program::pubkey::Pubkey) -> &mut Self {
        self.registration = Some(registration);
        self
    }
    /// Must match `registration.leader_wallet`.
    #[inline(always)]
    pub fn leader(&mut self, leader: solana_program::pubkey::Pubkey) -> &mut Self {
        self.leader = Some(leader);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: solana_program::instruction::AccountMeta,
    ) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_program::instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_program::instruction::Instruction {
        let accounts = RevokeCommunityCollection {
            registration: self.registration.expect("registration is not set"),
            leader: self.leader.expect("leader is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `revoke_community_collection` CPI accounts.
pub struct RevokeCommunityCollectionCpiAccounts<'a, 'b> {
    pub registration: &'b solana_program::account_info::AccountInfo<'a>,
    /// Must match `registration.leader_wallet`.
    pub leader: &'b solana_program::account_info::AccountInfo<'a>,
}

/// `revoke_community_collection` CPI instruction.
pub struct RevokeCommunityCollectionCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_program::account_info::AccountInfo<'a>,

    pub registration: &'b solana_program::account_info::AccountInfo<'a>,
    /// Must match `registration.leader_wallet`.
    pub leader: &'b solana_program::account_info::AccountInfo<'a>,
}

impl<'a, 'b> RevokeCommunityCollectionCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_program::account_info::AccountInfo<'a>,
        accounts: RevokeCommunityCollectionCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            registration: accounts.registration,
            leader: accounts.leader,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.registration.key,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            *self.leader.key,
            true,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_program::instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = RevokeCommunityCollectionInstructionData::new()
            .try_to_vec()
            .unwrap();

        let instruction = solana_program::instruction::Instruction {
            program_id: crate::TENSOR_MARKETPLACE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(2 + 1 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.registration.clone());
        account_infos.push(self.leader.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_program::program::invoke(&instruction, &account_infos)
        } else {
            solana_program::program::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `RevokeCommunityCollection` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` registration
///   1. `[signer]` leader
#[derive(Clone, Debug)]
pub struct RevokeCommunityCollectionCpiBuilder<'a, 'b> {
    instruction: Box<RevokeCommunityCollectionCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> RevokeCommunityCollectionCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_program::account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(RevokeCommunityCollectionCpiBuilderInstruction {
            __program: program,
            registration: None,
            leader: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn registration(
        &mut self,
        registration: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.registration = Some(registration);
        self
    }
    /// Must match `registration.leader_wallet`.
    #[inline(always)]
    pub fn leader(
        &mut self,
        leader: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.leader = Some(leader);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_program::account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        let instruction = RevokeCommunityCollectionCpi {
            __program: self.instruction.__program,

            registration: self
                .instruction
                .registration
                .expect("registration is not set"),

            leader: self.instruction.leader.expect("leader is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct RevokeCommunityCollectionCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_program::account_info::AccountInfo<'a>,
    registration: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    leader: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(
        &'b solana_program::account_info::AccountInfo<'a>,
        bool,
        bool,
    )>,
}
