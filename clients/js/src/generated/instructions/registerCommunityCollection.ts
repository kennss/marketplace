/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/web3.js';
import { findCommunityRegistrationPda } from '../pdas';
import { TENSOR_MARKETPLACE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const REGISTER_COMMUNITY_COLLECTION_DISCRIMINATOR = new Uint8Array([
  250, 58, 139, 204, 162, 147, 82, 44,
]);

export function getRegisterCommunityCollectionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_COMMUNITY_COLLECTION_DISCRIMINATOR
  );
}

export type RegisterCommunityCollectionInstruction<
  TProgram extends string = typeof TENSOR_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountRegistration extends string | IAccountMeta<string> = string,
  TAccountCollectionMint extends string | IAccountMeta<string> = string,
  TAccountMetadata extends string | IAccountMeta<string> = string,
  TAccountLeader extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountRegistration extends string
        ? WritableAccount<TAccountRegistration>
        : TAccountRegistration,
      TAccountCollectionMint extends string
        ? ReadonlyAccount<TAccountCollectionMint>
        : TAccountCollectionMint,
      TAccountMetadata extends string
        ? ReadonlyAccount<TAccountMetadata>
        : TAccountMetadata,
      TAccountLeader extends string
        ? WritableSignerAccount<TAccountLeader> &
            IAccountSignerMeta<TAccountLeader>
        : TAccountLeader,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterCommunityCollectionInstructionData = {
  discriminator: ReadonlyUint8Array;
  leaderSnowchatId: Array<number>;
  channelId: ReadonlyUint8Array;
};

export type RegisterCommunityCollectionInstructionDataArgs = {
  leaderSnowchatId: Array<number>;
  channelId: ReadonlyUint8Array;
};

export function getRegisterCommunityCollectionInstructionDataEncoder(): Encoder<RegisterCommunityCollectionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['leaderSnowchatId', getArrayEncoder(getU8Encoder(), { size: 36 })],
      ['channelId', fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({
      ...value,
      discriminator: REGISTER_COMMUNITY_COLLECTION_DISCRIMINATOR,
    })
  );
}

export function getRegisterCommunityCollectionInstructionDataDecoder(): Decoder<RegisterCommunityCollectionInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['leaderSnowchatId', getArrayDecoder(getU8Decoder(), { size: 36 })],
    ['channelId', fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getRegisterCommunityCollectionInstructionDataCodec(): Codec<
  RegisterCommunityCollectionInstructionDataArgs,
  RegisterCommunityCollectionInstructionData
> {
  return combineCodec(
    getRegisterCommunityCollectionInstructionDataEncoder(),
    getRegisterCommunityCollectionInstructionDataDecoder()
  );
}

export type RegisterCommunityCollectionAsyncInput<
  TAccountRegistration extends string = string,
  TAccountCollectionMint extends string = string,
  TAccountMetadata extends string = string,
  TAccountLeader extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Newly initialised PDA. Seed: [b"community_registration", collection_mint].
   * Leader pays rent.
   */
  registration?: Address<TAccountRegistration>;
  /** The collection master mint being registered. */
  collectionMint: Address<TAccountCollectionMint>;
  /**
   * Metaplex metadata account for `collection_mint`. Validated by
   * `assert_decode_metadata` and seeds-derived from collection_mint.
   */
  metadata: Address<TAccountMetadata>;
  /** Community leader — receives community share, pays rent, signs. */
  leader: TransactionSigner<TAccountLeader>;
  systemProgram?: Address<TAccountSystemProgram>;
  leaderSnowchatId: RegisterCommunityCollectionInstructionDataArgs['leaderSnowchatId'];
  channelId: RegisterCommunityCollectionInstructionDataArgs['channelId'];
};

export async function getRegisterCommunityCollectionInstructionAsync<
  TAccountRegistration extends string,
  TAccountCollectionMint extends string,
  TAccountMetadata extends string,
  TAccountLeader extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TENSOR_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: RegisterCommunityCollectionAsyncInput<
    TAccountRegistration,
    TAccountCollectionMint,
    TAccountMetadata,
    TAccountLeader,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RegisterCommunityCollectionInstruction<
    TProgramAddress,
    TAccountRegistration,
    TAccountCollectionMint,
    TAccountMetadata,
    TAccountLeader,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TENSOR_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    registration: { value: input.registration ?? null, isWritable: true },
    collectionMint: { value: input.collectionMint ?? null, isWritable: false },
    metadata: { value: input.metadata ?? null, isWritable: false },
    leader: { value: input.leader ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.registration.value) {
    accounts.registration.value = await findCommunityRegistrationPda({
      collectionMint: expectAddress(accounts.collectionMint.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.registration),
      getAccountMeta(accounts.collectionMint),
      getAccountMeta(accounts.metadata),
      getAccountMeta(accounts.leader),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getRegisterCommunityCollectionInstructionDataEncoder().encode(
      args as RegisterCommunityCollectionInstructionDataArgs
    ),
  } as RegisterCommunityCollectionInstruction<
    TProgramAddress,
    TAccountRegistration,
    TAccountCollectionMint,
    TAccountMetadata,
    TAccountLeader,
    TAccountSystemProgram
  >;

  return instruction;
}

export type RegisterCommunityCollectionInput<
  TAccountRegistration extends string = string,
  TAccountCollectionMint extends string = string,
  TAccountMetadata extends string = string,
  TAccountLeader extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Newly initialised PDA. Seed: [b"community_registration", collection_mint].
   * Leader pays rent.
   */
  registration: Address<TAccountRegistration>;
  /** The collection master mint being registered. */
  collectionMint: Address<TAccountCollectionMint>;
  /**
   * Metaplex metadata account for `collection_mint`. Validated by
   * `assert_decode_metadata` and seeds-derived from collection_mint.
   */
  metadata: Address<TAccountMetadata>;
  /** Community leader — receives community share, pays rent, signs. */
  leader: TransactionSigner<TAccountLeader>;
  systemProgram?: Address<TAccountSystemProgram>;
  leaderSnowchatId: RegisterCommunityCollectionInstructionDataArgs['leaderSnowchatId'];
  channelId: RegisterCommunityCollectionInstructionDataArgs['channelId'];
};

export function getRegisterCommunityCollectionInstruction<
  TAccountRegistration extends string,
  TAccountCollectionMint extends string,
  TAccountMetadata extends string,
  TAccountLeader extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TENSOR_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: RegisterCommunityCollectionInput<
    TAccountRegistration,
    TAccountCollectionMint,
    TAccountMetadata,
    TAccountLeader,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): RegisterCommunityCollectionInstruction<
  TProgramAddress,
  TAccountRegistration,
  TAccountCollectionMint,
  TAccountMetadata,
  TAccountLeader,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TENSOR_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    registration: { value: input.registration ?? null, isWritable: true },
    collectionMint: { value: input.collectionMint ?? null, isWritable: false },
    metadata: { value: input.metadata ?? null, isWritable: false },
    leader: { value: input.leader ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.registration),
      getAccountMeta(accounts.collectionMint),
      getAccountMeta(accounts.metadata),
      getAccountMeta(accounts.leader),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getRegisterCommunityCollectionInstructionDataEncoder().encode(
      args as RegisterCommunityCollectionInstructionDataArgs
    ),
  } as RegisterCommunityCollectionInstruction<
    TProgramAddress,
    TAccountRegistration,
    TAccountCollectionMint,
    TAccountMetadata,
    TAccountLeader,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedRegisterCommunityCollectionInstruction<
  TProgram extends string = typeof TENSOR_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Newly initialised PDA. Seed: [b"community_registration", collection_mint].
     * Leader pays rent.
     */

    registration: TAccountMetas[0];
    /** The collection master mint being registered. */
    collectionMint: TAccountMetas[1];
    /**
     * Metaplex metadata account for `collection_mint`. Validated by
     * `assert_decode_metadata` and seeds-derived from collection_mint.
     */

    metadata: TAccountMetas[2];
    /** Community leader — receives community share, pays rent, signs. */
    leader: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RegisterCommunityCollectionInstructionData;
};

export function parseRegisterCommunityCollectionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedRegisterCommunityCollectionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      registration: getNextAccount(),
      collectionMint: getNextAccount(),
      metadata: getNextAccount(),
      leader: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterCommunityCollectionInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
