/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/web3.js';
import { TENSOR_MARKETPLACE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const REVOKE_COMMUNITY_COLLECTION_DISCRIMINATOR = new Uint8Array([
  183, 235, 152, 95, 37, 136, 110, 172,
]);

export function getRevokeCommunityCollectionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKE_COMMUNITY_COLLECTION_DISCRIMINATOR
  );
}

export type RevokeCommunityCollectionInstruction<
  TProgram extends string = typeof TENSOR_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountRegistration extends string | IAccountMeta<string> = string,
  TAccountLeader extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountRegistration extends string
        ? WritableAccount<TAccountRegistration>
        : TAccountRegistration,
      TAccountLeader extends string
        ? ReadonlySignerAccount<TAccountLeader> &
            IAccountSignerMeta<TAccountLeader>
        : TAccountLeader,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeCommunityCollectionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevokeCommunityCollectionInstructionDataArgs = {};

export function getRevokeCommunityCollectionInstructionDataEncoder(): Encoder<RevokeCommunityCollectionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REVOKE_COMMUNITY_COLLECTION_DISCRIMINATOR,
    })
  );
}

export function getRevokeCommunityCollectionInstructionDataDecoder(): Decoder<RevokeCommunityCollectionInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevokeCommunityCollectionInstructionDataCodec(): Codec<
  RevokeCommunityCollectionInstructionDataArgs,
  RevokeCommunityCollectionInstructionData
> {
  return combineCodec(
    getRevokeCommunityCollectionInstructionDataEncoder(),
    getRevokeCommunityCollectionInstructionDataDecoder()
  );
}

export type RevokeCommunityCollectionInput<
  TAccountRegistration extends string = string,
  TAccountLeader extends string = string,
> = {
  registration: Address<TAccountRegistration>;
  /** Must match `registration.leader_wallet`. */
  leader: TransactionSigner<TAccountLeader>;
};

export function getRevokeCommunityCollectionInstruction<
  TAccountRegistration extends string,
  TAccountLeader extends string,
  TProgramAddress extends Address = typeof TENSOR_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: RevokeCommunityCollectionInput<TAccountRegistration, TAccountLeader>,
  config?: { programAddress?: TProgramAddress }
): RevokeCommunityCollectionInstruction<
  TProgramAddress,
  TAccountRegistration,
  TAccountLeader
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TENSOR_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    registration: { value: input.registration ?? null, isWritable: true },
    leader: { value: input.leader ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.registration),
      getAccountMeta(accounts.leader),
    ],
    programAddress,
    data: getRevokeCommunityCollectionInstructionDataEncoder().encode({}),
  } as RevokeCommunityCollectionInstruction<
    TProgramAddress,
    TAccountRegistration,
    TAccountLeader
  >;

  return instruction;
}

export type ParsedRevokeCommunityCollectionInstruction<
  TProgram extends string = typeof TENSOR_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    registration: TAccountMetas[0];
    /** Must match `registration.leader_wallet`. */
    leader: TAccountMetas[1];
  };
  data: RevokeCommunityCollectionInstructionData;
};

export function parseRevokeCommunityCollectionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedRevokeCommunityCollectionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      registration: getNextAccount(),
      leader: getNextAccount(),
    },
    data: getRevokeCommunityCollectionInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
