/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from '@solana/web3.js';
import {
  CommunityRegistrationSeeds,
  findCommunityRegistrationPda,
} from '../pdas';

export const COMMUNITY_REGISTRATION_DISCRIMINATOR = new Uint8Array([
  236, 180, 10, 39, 116, 172, 226, 118,
]);

export function getCommunityRegistrationDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    COMMUNITY_REGISTRATION_DISCRIMINATOR
  );
}

export type CommunityRegistration = {
  discriminator: ReadonlyUint8Array;
  /** Schema version. Bump on layout-incompatible changes. */
  version: number;
  /** PDA bump. */
  bump: number;
  /** NFT collection master mint (PDA seed). */
  collectionMint: Address;
  /**
   * Verified leader wallet — recipient of community share. Must match
   * `metadata.update_authority` at registration time AND must appear as
   * a `verified=true` entry in `metadata.creators[]` on every trade
   * (cross-checked in `apply_community_share`).
   */
  leaderWallet: Address;
  /**
   * SnowChat ID snapshot for off-chain audit trail.
   * Format: ASCII "snow" + 32 hex chars = 36 bytes.
   */
  leaderSnowchatId: Array<number>;
  /**
   * SnowChat channel id snapshot (off-chain index helper). Server side
   * enforces 1 collection per channel via DB unique constraint; on-chain
   * only stores for emit/audit.
   */
  channelId: ReadonlyUint8Array;
  /**
   * SHA-256 hash of the metadata account at registration time. Re-checked
   * on every trade to detect post-registration mutation
   * (Agent A P1-3 / Agent B P1 S-3 — metadata mutation attack).
   */
  metadataHash: ReadonlyUint8Array;
  /** unix timestamp. */
  registeredAt: bigint;
  /**
   * None = active. Some(ts) = revoked at ts. Soft delete preserves
   * cumulative stats + audit trail.
   */
  revokedAt: Option<bigint>;
  /**
   * Reason code for revocation. Enum variants:
   * 0 = user_request
   * 1 = metadata_mismatch (auto-suspended on hash mismatch)
   * 2 = admin_action
   * 3 = cooldown_violation
   * 4 = update_authority_changed
   */
  revokedReason: number;
  /**
   * Lamports of community share routed cumulative. **SOL flows only.**
   * Atomic increment on every successful SOL-currency Buy/TakeBid via
   * `record_share_distribution`. SPL currency (USDC/TNSR/etc.) Buy
   * flows intentionally do NOT increment this counter — mixing
   * different smallest-unit scales in the same `u64` would corrupt
   * monthly settlement and audit (see Agent A P0-1 / §17.11). SPL
   * aggregation lives off-chain in indexer events.
   */
  cumulativeShareLamports: bigint;
  /**
   * Number of trades that routed share to this registration.
   * **SOL flows only** for the same reason as `cumulative_share_lamports`
   * — incrementing on SPL flows would make the counter semantically
   * ambiguous when reconciling against lamport totals.
   */
  tradeCount: bigint;
  /**
   * Reserved for forward compatibility (e.g. tier-based split ratios,
   * per-currency cumulative tracking). Zero-filled at init.
   */
  reserved: ReadonlyUint8Array;
};

export type CommunityRegistrationArgs = {
  /** Schema version. Bump on layout-incompatible changes. */
  version: number;
  /** PDA bump. */
  bump: number;
  /** NFT collection master mint (PDA seed). */
  collectionMint: Address;
  /**
   * Verified leader wallet — recipient of community share. Must match
   * `metadata.update_authority` at registration time AND must appear as
   * a `verified=true` entry in `metadata.creators[]` on every trade
   * (cross-checked in `apply_community_share`).
   */
  leaderWallet: Address;
  /**
   * SnowChat ID snapshot for off-chain audit trail.
   * Format: ASCII "snow" + 32 hex chars = 36 bytes.
   */
  leaderSnowchatId: Array<number>;
  /**
   * SnowChat channel id snapshot (off-chain index helper). Server side
   * enforces 1 collection per channel via DB unique constraint; on-chain
   * only stores for emit/audit.
   */
  channelId: ReadonlyUint8Array;
  /**
   * SHA-256 hash of the metadata account at registration time. Re-checked
   * on every trade to detect post-registration mutation
   * (Agent A P1-3 / Agent B P1 S-3 — metadata mutation attack).
   */
  metadataHash: ReadonlyUint8Array;
  /** unix timestamp. */
  registeredAt: number | bigint;
  /**
   * None = active. Some(ts) = revoked at ts. Soft delete preserves
   * cumulative stats + audit trail.
   */
  revokedAt: OptionOrNullable<number | bigint>;
  /**
   * Reason code for revocation. Enum variants:
   * 0 = user_request
   * 1 = metadata_mismatch (auto-suspended on hash mismatch)
   * 2 = admin_action
   * 3 = cooldown_violation
   * 4 = update_authority_changed
   */
  revokedReason: number;
  /**
   * Lamports of community share routed cumulative. **SOL flows only.**
   * Atomic increment on every successful SOL-currency Buy/TakeBid via
   * `record_share_distribution`. SPL currency (USDC/TNSR/etc.) Buy
   * flows intentionally do NOT increment this counter — mixing
   * different smallest-unit scales in the same `u64` would corrupt
   * monthly settlement and audit (see Agent A P0-1 / §17.11). SPL
   * aggregation lives off-chain in indexer events.
   */
  cumulativeShareLamports: number | bigint;
  /**
   * Number of trades that routed share to this registration.
   * **SOL flows only** for the same reason as `cumulative_share_lamports`
   * — incrementing on SPL flows would make the counter semantically
   * ambiguous when reconciling against lamport totals.
   */
  tradeCount: number | bigint;
  /**
   * Reserved for forward compatibility (e.g. tier-based split ratios,
   * per-currency cumulative tracking). Zero-filled at init.
   */
  reserved: ReadonlyUint8Array;
};

export function getCommunityRegistrationEncoder(): Encoder<CommunityRegistrationArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['version', getU8Encoder()],
      ['bump', getU8Encoder()],
      ['collectionMint', getAddressEncoder()],
      ['leaderWallet', getAddressEncoder()],
      ['leaderSnowchatId', getArrayEncoder(getU8Encoder(), { size: 36 })],
      ['channelId', fixEncoderSize(getBytesEncoder(), 32)],
      ['metadataHash', fixEncoderSize(getBytesEncoder(), 32)],
      ['registeredAt', getI64Encoder()],
      ['revokedAt', getOptionEncoder(getI64Encoder())],
      ['revokedReason', getU8Encoder()],
      ['cumulativeShareLamports', getU64Encoder()],
      ['tradeCount', getU64Encoder()],
      ['reserved', fixEncoderSize(getBytesEncoder(), 64)],
    ]),
    (value) => ({
      ...value,
      discriminator: COMMUNITY_REGISTRATION_DISCRIMINATOR,
    })
  );
}

export function getCommunityRegistrationDecoder(): Decoder<CommunityRegistration> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['version', getU8Decoder()],
    ['bump', getU8Decoder()],
    ['collectionMint', getAddressDecoder()],
    ['leaderWallet', getAddressDecoder()],
    ['leaderSnowchatId', getArrayDecoder(getU8Decoder(), { size: 36 })],
    ['channelId', fixDecoderSize(getBytesDecoder(), 32)],
    ['metadataHash', fixDecoderSize(getBytesDecoder(), 32)],
    ['registeredAt', getI64Decoder()],
    ['revokedAt', getOptionDecoder(getI64Decoder())],
    ['revokedReason', getU8Decoder()],
    ['cumulativeShareLamports', getU64Decoder()],
    ['tradeCount', getU64Decoder()],
    ['reserved', fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

export function getCommunityRegistrationCodec(): Codec<
  CommunityRegistrationArgs,
  CommunityRegistration
> {
  return combineCodec(
    getCommunityRegistrationEncoder(),
    getCommunityRegistrationDecoder()
  );
}

export function decodeCommunityRegistration<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<CommunityRegistration, TAddress>;
export function decodeCommunityRegistration<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<CommunityRegistration, TAddress>;
export function decodeCommunityRegistration<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
):
  | Account<CommunityRegistration, TAddress>
  | MaybeAccount<CommunityRegistration, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getCommunityRegistrationDecoder()
  );
}

export async function fetchCommunityRegistration<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<CommunityRegistration, TAddress>> {
  const maybeAccount = await fetchMaybeCommunityRegistration(
    rpc,
    address,
    config
  );
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCommunityRegistration<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<CommunityRegistration, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCommunityRegistration(maybeAccount);
}

export async function fetchAllCommunityRegistration(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<CommunityRegistration>[]> {
  const maybeAccounts = await fetchAllMaybeCommunityRegistration(
    rpc,
    addresses,
    config
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCommunityRegistration(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<CommunityRegistration>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeCommunityRegistration(maybeAccount)
  );
}

export async function fetchCommunityRegistrationFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: CommunityRegistrationSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {}
): Promise<Account<CommunityRegistration>> {
  const maybeAccount = await fetchMaybeCommunityRegistrationFromSeeds(
    rpc,
    seeds,
    config
  );
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCommunityRegistrationFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: CommunityRegistrationSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {}
): Promise<MaybeAccount<CommunityRegistration>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findCommunityRegistrationPda(seeds, {
    programAddress,
  });
  return await fetchMaybeCommunityRegistration(rpc, address, fetchConfig);
}
